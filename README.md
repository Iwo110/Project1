# MultiTool Skeleton

This project demonstrates a simple structure for a Python command-line application with multiple subcommands.

## Usage

```bash
python main.py greet Alice
python main.py calc 1 2
```

## Available commands

- `greet NAME` - prints a friendly greeting.
- `calc A B` - displays the sum of two integers.
